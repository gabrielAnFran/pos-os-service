apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-os-service-worker
  labels:
    app.kubernetes.io/name: os-service-worker
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: os-service-worker
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: os-service-worker
        app.kubernetes.io/instance: {{ .Release.Name }}
    spec:
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tags.worker }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ .Release.Name }}-os-service-config
            - secretRef:
                name: {{ .Release.Name }}-os-service-secret
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
